// config.example.js
// Copy this file to `config.js` and replace the placeholder with your real API key.
// Do NOT commit `config.js` — it should remain local and is added to .gitignore.

window.APP_CONFIG = {
  // Replace the string below with your OpenWeatherMap API key
  apiKey: "YOUR_OPENWEATHERMAP_API_KEY_HERE"
};
